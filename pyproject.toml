[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["fewsxml/tests"]
python_files = ["test_*.py"]
norecursedirs = ["run_dir_tmp", "build", "*.egg-info"]
